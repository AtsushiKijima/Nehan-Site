# Nehan サイト アップロード手順（Xserver / FTP）

## できているもの

| ローカルのフォルダ | これは何か |
|---|---|
| `nehan-theme/` | 会社サイト（トップ）の **WordPress テーマ**。お知らせは投稿と連動。 |
| `recruit-static/` | リクルートページ（**静的HTML**）。`/recruit/` で公開。 |
| `mock/` | 元のデザインHTML（アップロード不要・保管用） |

---

## アップロード先の対応表（FileZilla の右側＝サーバー）

| ローカル | → | サーバー上の置き場所 |
|---|---|---|
| `nehan-theme/` フォルダごと | → | `（ドメイン）/public_html/wp-content/themes/nehan-theme/` |
| `recruit-static/` の**中身** | → | `（ドメイン）/public_html/recruit/` |

> ⚠️ recruit は「`recruit-static` というフォルダ名」ではなく、**中身を `recruit/` という名前の場所に入れる**点に注意（フォルダ名が `recruit` になればOK）。

---

## 手順

### A. テーマ（会社サイト）

1. FileZilla でサーバーに接続（前回の手順）
2. 右ペインで `public_html/wp-content/themes/` を開く
3. ローカルの `nehan-theme` フォルダを **まるごとドラッグ&ドロップ**
4. WordPress 管理画面 →「外観 → テーマ」→ **「Nehan Theme」を有効化**
5. トップページ（`https://ドメイン/`）を開いて表示確認

### B. リクルート（静的）

1. 右ペインで `public_html/` を開く
2. **新規フォルダ `recruit` を作成**
3. ローカル `recruit-static/` の中身（`index.html` と `photos/`、画像一式）を `recruit/` にドラッグ&ドロップ
4. `https://ドメイン/recruit/` を開いて表示確認

### C. 画像（あとで用意したら）

- 会社サイト用 → `nehan-theme/recruit/` と `nehan-theme/executives/` に入れて再アップ
  （必要な6枚は `nehan-theme/_画像をここに入れる.md` 参照）
- リクルート用 → `recruit/` 直下と `recruit/photos/` に入れて再アップ
  （必要な画像は `recruit-static/_画像をここに入れる.md` 参照）

---

## アップロード後にやること（WordPress）

### お知らせを投稿する
管理画面 →「投稿 → 新規追加」→ タイトル・本文を書いて公開
→ トップページの **News セクションに自動で並ぶ**（新しい順・最大5件）
→ クリックすると記事の個別ページが開く

### サンプルを消す
最初から入っている「Hello world!」の投稿は、「投稿一覧」から削除してOK

### トップページの設定（重要）
「Hello world」がトップに出てしまう場合：
管理画面 →「設定 → 表示設定」→「ホームページの表示」を確認。
**このテーマは `front-page.php` があるので、設定に関わらずトップは自動でデザイン版になります**が、
もし違う表示になる場合はここで「最新の投稿」を選べばOK。

---

## トラブル時

- **画面が真っ白** → テーマのPHPファイルのどれかが壊れている可能性。FTPで再アップロード。
- **デザインが崩れて文字だけ** → `style.css` か `functions.php` のアップロード漏れ。両方あるか確認。
- **画像だけ✕** → 画像ファイル未配置（仕様通り。画像を入れれば直る）。
- **変更が反映されない** → ブラウザのスーパーリロード（`Ctrl + F5`）。